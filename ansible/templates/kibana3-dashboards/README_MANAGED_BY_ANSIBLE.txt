# {{ ansible_managed }}
# These dashboards are managed by ansible, but json does not have comments so can't add a comment in there with ansible_managed like above..
# Not all are overwritten, for example not default.json and logstash.js
